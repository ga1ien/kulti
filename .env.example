# =============================================================================
# KULTI ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env.local for local development
# Copy to .env.production for production deployment
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: Supabase Configuration
# -----------------------------------------------------------------------------
# Database and authentication provider
# Get these from: https://app.supabase.com/project/_/settings/api

NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Optional: Only needed for admin database operations via Supabase CLI
SUPABASE_ACCESS_TOKEN=your_supabase_access_token

# -----------------------------------------------------------------------------
# REQUIRED: 100ms HMS Configuration
# -----------------------------------------------------------------------------
# Video conferencing infrastructure
# Get these from: https://dashboard.100ms.live/

NEXT_PUBLIC_HMS_APP_ID=your_hms_app_id
HMS_APP_ACCESS_KEY=your_hms_access_key
HMS_APP_SECRET=your_hms_app_secret
HMS_TEMPLATE_ID=your_hms_template_id

# HLS Streaming Threshold (Optional)
# When a session reaches this many participants, new viewers automatically
# switch to HLS streaming for better scalability (hosts/presenters stay on WebRTC)
# Default: 100 participants
# HLS_THRESHOLD=100

# -----------------------------------------------------------------------------
# REQUIRED: Anthropic AI Configuration
# -----------------------------------------------------------------------------
# AI chat and conversation features
# Get this from: https://console.anthropic.com/

ANTHROPIC_API_KEY=your_anthropic_api_key

# -----------------------------------------------------------------------------
# REQUIRED: Application Configuration
# -----------------------------------------------------------------------------
# Base URL for your application

# Development
NEXT_PUBLIC_APP_URL=http://localhost:3002

# Production (replace with your domain)
# NEXT_PUBLIC_APP_URL=https://kulti.app

# Environment type
NODE_ENV=development

# -----------------------------------------------------------------------------
# OPTIONAL: Upstash Redis (Rate Limiting)
# -----------------------------------------------------------------------------
# Production rate limiting - recommended for production
# Without this, app uses in-memory rate limiting (not suitable for production)
# Sign up at: https://upstash.com

# UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url
# UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token

# -----------------------------------------------------------------------------
# OPTIONAL: Sentry (Error Tracking)
# -----------------------------------------------------------------------------
# Production error tracking and performance monitoring
# Highly recommended for production
# Sign up at: https://sentry.io

# NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn

# -----------------------------------------------------------------------------
# OPTIONAL: Twilio (SMS/Phone Auth)
# -----------------------------------------------------------------------------
# Only needed if using SMS-based authentication
# Currently using Supabase phone auth

# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token
# TWILIO_PHONE_NUMBER=your_twilio_phone_number

# =============================================================================
# PRODUCTION CHECKLIST
# =============================================================================
# Before deploying to production, ensure:
#
# ✅ All REQUIRED variables are set
# ✅ NEXT_PUBLIC_APP_URL uses HTTPS
# ✅ Sentry DSN is configured for error tracking
# ✅ Upstash Redis is configured for rate limiting
# ✅ All secrets are stored securely (not in git)
# ✅ Environment variables are set in Vercel/hosting platform
# ✅ HMS webhook URL is configured in HMS dashboard
# ✅ Supabase RLS policies are enabled
# ✅ Database backups are configured
# =============================================================================
