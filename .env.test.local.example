# ============================================
# Staging/Production-like Test Environment
# ============================================
# Copy this to .env.test.local and fill in real values for integration testing
# This file is used when you need to test against real services

# ============================================
# Supabase - Staging Environment
# ============================================
# Use your staging Supabase project credentials
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-staging-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_staging_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_staging_service_role_key

# ============================================
# 100ms - Test Template
# ============================================
# Use a dedicated test template for E2E testing
# Get these from: https://dashboard.100ms.live/
NEXT_PUBLIC_HMS_APP_ID=your_test_app_id
HMS_APP_ACCESS_KEY=your_test_access_key
HMS_APP_SECRET=your_test_secret
HMS_TEMPLATE_ID=your_test_template_id
HMS_MANAGEMENT_TOKEN=your_test_management_token

# ============================================
# Application Configuration
# ============================================
NEXT_PUBLIC_APP_URL=http://localhost:5555
NODE_ENV=test

# ============================================
# Optional Services
# ============================================
# AI Transcription (Optional)
ANTHROPIC_API_KEY=your_anthropic_key

# Rate Limiting (Optional)
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_token

# ============================================
# Setup Instructions
# ============================================
# 1. Copy this file: cp .env.test.local.example .env.test.local
# 2. Fill in your staging/test credentials above
# 3. Run integration tests: npm run test:e2e:staging
# 4. NEVER commit .env.test.local (it's gitignored)
#
# For most E2E tests, the default .env.test with mock values is sufficient.
# Use .env.test.local only when testing features that require real services:
#   - Video streaming (requires real HMS credentials)
#   - Database operations (requires real Supabase)
#   - AI transcription (requires real Anthropic API)
